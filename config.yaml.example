SECRET_KEY: "replace this with a long random string"
# APPLICATION_ROOT: /quickcategories/ # only on Toolforge (used for the session cookie path)
oauth:
    consumer_key: ...
    consumer_secret: ...
# database:
#     user: ...
#     password: ...
#     db: ...
#     read_default_file: ~/replica.my.cnf # on Toolforge, instead of user and password
